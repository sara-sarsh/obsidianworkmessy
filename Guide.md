# راهنمای کامل پروژه یوتیوب دانلودر

به دنیای برنامه‌نویسی خوش آمدید! این راهنما برای کسی نوشته شده که می‌خواهد بداند دقیقاً در این پروژه چه اتفاقی می‌افتد. ما از **پایتون** و **استریم‌لیت** استفاده کرده‌ایم.

---

## ۱. مفاهیم پایه

### پایتون (Python) چیست؟
پایتون یک زبان برنامه‌نویسی است که به دو ویژگی معروف است:
1.  **خوانایی بالا**: کدهای پایتون بسیار شبیه به زبان انگلیسی هستند.
2.  **کتابخانه‌های قدرتمند**: برای تقریبا هر کاری (هوش مصنوعی، وب، دانلود، و...) ابزار آماده دارد.

### استریم‌لیت (Streamlit) چیست؟
استریم‌لیت یک کتابخانه پایتون است که به شما اجازه می‌دهد **بدون نوشتن HTML یا CSS**، وب‌اپلیکیشن‌های زیبا بسازید. همه چیز با دستورات ساده پایتون کنترل می‌شود.

---

## ۲. ساختار پروژه

```
Youtube downloader/
├── app.py              # کد اصلی برنامه
├── requirements.txt    # لیست کتابخانه‌های مورد نیاز
└── downloads/          # پوشه‌ای که فایل‌ها در آن ذخیره می‌شوند
```

---

## ۳. تحلیل خط به خط کد (app.py)

بیایید کد را کالبدشکافی کنیم.

### بخش اول: وارد کردن ابزارها (Imports)
```python
import streamlit as st
import yt_dlp
import os
```
*   `import ...`: دستور فراخوانی کتابخانه‌هاست.
*   `streamlit as st`: یعنی کتابخانه استریم‌لیت را بیاور، ولی برای راحتی به آن `st` می‌گوییم. هر جا `st` دیدید، یعنی دستوری مربوط به ظاهر برنامه.
*   `yt_dlp`: موتور قدرتمند دانلودر ماست. این کتابخانه مسئولیت ارتباط با یوتیوب و دانلود را دارد.
*   `os`: کتابخانه‌ای برای کار با سیستم عامل (ساخت پوشه، چک کردن فایل‌ها و...).

### بخش دوم: تنظیمات صفحه
```python
st.set_page_config(page_title="Simple YouTube Downloader", page_icon="⬇️")
```
*   `set_page_config`: عنوان تب مرورگر و آیکون آن را تنظیم می‌کند.

### بخش سوم: عنوان‌ها و ورودی‌ها
```python
st.title("📺 Simple YouTube Downloader")
st.write("Enter YouTube URLs (one per line)...")
```
*   `st.title`: یک تیتر بزرگ (H1) می‌سازد.
*   `st.write`: متن معمولی را نمایش می‌دهد.

```python
urls_input = st.text_area(
    "YouTube Video URLs (one per line)",
    placeholder="https://...",
    height=150
)
```
*   `st.text_area`: یک کادر متنی بزرگ می‌سازد (مناسب برای چند خط متن).
*   `placeholder`: متنی که وقتی کادر خالی است، کم‌رنگ نمایش داده می‌شود تا کاربر بفهمد چه باید تایپ کند.

### بخش چهارم: پردازش متن ورودی
```python
urls = [url.strip() for url in urls_input.strip().split('\n') if url.strip()]
```
این یک **List Comprehension** (مفهوم پیشرفته پایتون) است. بیایید بازش کنیم:
1.  `urls_input.strip()`: فاصله‌های اضافی اول و آخر کل متن را حذف می‌کند.
2.  `.split('\n')`: متن را از جایی که اینتر زده شده (خط جدید) تکه تکه می‌کند و به یک لیست تبدیل می‌کند.
3.  `for url in ...`: روی هر تکه (هر لینک) می‌چرخد.
4.  `if url.strip()`: اگر خط خالی نبود (کاربر الکی اینتر نزن باشد)، آن را نگه می‌دارد.
5.  در نهایت `urls` لیست تمیزی از لینک‌هاست.

### بخش پنجم: انتخاب فرمت
```python
format_choice = st.selectbox(
    "Select Download Format",
    ["Option 1", "Option 2", ...]
)
```
*   `st.selectbox`: یک منوی کشویی (Dropdown) می‌سازد. مقدار انتخاب شده در متغیر `format_choice` ریخته می‌شود.

### بخش ششم: دیکشنری نگاشت (Mapping)
```python
format_map = {
    "Best Quality (Video + Audio)": "bestvideo+bestaudio/best",
    ...
}
```
*   **دیکشنری (Dictionary)**: ساختاری که کلید (Key) را به مقدار (Value) وصل می‌کند.
*   ما متن قابل فهم برای کاربر (مثلاً "Best Quality") را به دستورات فنی `yt-dlp` (مثلاً `bestvideo+bestaudio/best`) تبدیل می‌کنیم.
    *   `bestvideo+bestaudio/best`: یعنی سعی کن بهترین ویدیو و بهترین صدا را جدا دانلود کنی و بچسبانی، اگر نشد بهترین فایل تکی را دانلود کن.
    *   `[height<=720]`: شرط می‌گذاریم که ارتفاع (کیفیت) تصویر بیشتر از ۷۲۰ نباشد.

### بخش هفتم: دکمه و منطق دانلود
```python
if st.button("Download All"):
    # کد زیر فقط وقتی اجرا می‌شود که دکمه کلیک شود
```

#### حلقه (Loop) روی لینک‌ها
```python
for i, url in enumerate(urls):
    ...
```
*   `enumerate(urls)`: هم خود لینک را می‌دهد (`url`) و هم شماره‌اش را (`i`). ما از `i` برای نمایش پیشرفت کلی استفاده می‌کنیم.

#### هوک پیشرفت (Progress Hook)
```python
def progress_hook(d):
    if d['status'] == 'downloading':
        ...
```
*   این یک **تابع (Function)** است که ما به `yt-dlp` می‌دهیم.
*   `yt-dlp` هر چند لحظه یکبار این تابع را صدا می‌زند و وضعیت (`d`) را گزارش می‌دهد. ما از این گزارش برای پر کردن نوار پیشرفت (`st.progress`) استفاده می‌کنیم.

#### تنظیمات دانلودر (Options)
```python
ydl_opts = {
    'format': format_map[format_choice],
    'outtmpl': f'{output_dir}/%(title)s.%(ext)s',
    ...
}
```
*   `outtmpl`: الگوی نام‌گذاری فایل خروجی. `%(title)s` یعنی نام ویدیو را بگذار نام فایل.

#### بلوک try/except
```python
try:
    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        ydl.extract_info(url, download=True)
except Exception as e:
    st.error(...)
```
*   **مدیریت خطا**: هر چیزی که در بلوک `try` باشد اجرا می‌شود. اگر اروری پیش بیاید (مثلا اینترنت قطع شود یا لینک خراب باشد)، برنامه کرش نمی‌کند، بلکه به بلوک `except` می‌رود و پیغام خطا نشان می‌دهد.

---

## ۴. راهنمای اجرا

برای اجرای این برنامه روی سیستم خودتان:

1.  **نصب پیش‌نیازها**: مطمئن شوید پایتون نصب است. سپس در ترمینال (Terminal) یا خط فرمان (CMD) در پوشه پروژه بنویسید:
    ```bash
    pip install -r requirements.txt
    ```

2.  **اجرای برنامه**:
    ```bash
    streamlit run app.py
    ```

3.  **استفاده**:
    *   لینک‌ها را در کادر وارد کنید.
    *   فرمت را انتخاب کنید (مثلاً MP3 برای آهنگ).
    *   دکمه Download All را بزنید.
    *   فایل‌ها در پوشه `downloads` در کنار فایل `app.py` ذخیره می‌شوند.

---

## ۵. بخش ابزارهای جانبی (Sidebar)

در گوشه صفحه (سایدبار)، دو قابلیت مهم اضافه شده است:
1.  **پاک‌سازی حافظه (Clear Cache)**: اگر با خطای 403 مواجه شدید، این دکمه را بزنید.
2.  **مشاهده مسیر ذخیره‌سازی**: مسیر کامل پوشه‌ای که فایل‌ها در آن دانلود می‌شوند نمایش داده شده است.
3.  **دکمه باز کردن پوشه**: با زدن این دکمه، پوشه `downloads` مستقیماً در سیستم شما باز می‌شود تا نخواهید دنبال فایل‌ها بگردید.
4.  **بررسی فایل‌های تکراری**: قبل از شروع دانلود، برنامه چک می‌کند که آیا ویدیو قبلاً دانلود شده است یا خیر:
    *   اگر دقیقاً همان فایل وجود داشته باشد، از دانلود صرف‌نظر (Skip) می‌کند.
    *   اگر ویدیو با فرمت دیگری (مثلاً به جای ویدیو، نسخه صوتی آن) وجود داشته باشد، به شما هشدار می‌دهد ولی دانلود را انجام می‌دهد.

---

## ۶. نکات پیشرفته برای شما

*   **FFmpeg (ضروری)**: برای چسباندن (Merge) ویدیو و صدا در کیفیت‌های بالا (مثل 1080p و بالاتر) و همچنین برای تبدیل ویدیو به MP3، سیستم به ابزاری به نام `ffmpeg` نیاز دارد. 
    *   **وضعیت فعلی**: این ابزار قبلاً روی سیستم شما نصب نبود که باعث بروز خطا در دانلودهای با کیفیت بالا می‌شد. من آن را با استفاده از Homebrew نصب کردم. حالا برنامه می‌تواند بدون مشکل فایل‌ها را ترکیب و به MP3 تبدیل کند.

موفق باشید!

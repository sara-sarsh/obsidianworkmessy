با بررسی دقیق لاگ‌ها، با یک باگ زنجیره‌ای (Cascading Bug) روبرو هستیم که ریشه اصلی آن به مدیریت همزمانی و مسدود شدن لوپ ناهمگام پایتون (`asyncio`) برمی‌گردد.

در ادامه، تحلیل دقیق اتفاقی که افتاده و دلیل کرش کردن اتصال و خطای بعد از آن را توضیح می‌دهم:

---

## 🔍 ریشه‌یابی باگ (چه اتفاقی افتاد؟)

### ۱. شروع ماجرا: تداخل درخواست‌های همزمان (Race Condition)

در ساعت `00:34:10` وضعیت سخت‌افزار اسلحه شماره ۱ به `CHARGING` تغییر می‌کند. دقیقاً در همین ثانیه، دو اتفاق همزمان رخ می‌دهد:

* متد داخلی شما مجدداً تلاش می‌کند یک اکشن `start_transaction` جدید برای کانکتور ۱ بسازد که با اخطار زیر مواجه می‌شود (چون تراکنش از قبل فعال بوده):
> `[START] Connector 1 already has an active transaction: 593602327`


* دقیقاً در همین لحظه، تسک هارت‌بیت هم می‌خواهد پیام `Heartbeat` را به سرور بفرستد.

### ۲. قطع شدن وب‌ساکت (WS Connection lost)

به دلیل حجم تسک‌های همزمان روی کانکتور لندر یا پردازش مسدودکننده، کلاینت نمی‌تواند در پنجره زمانی تعیین شده (`ping_timeout`) به پینگ‌های سرور پاسخ دهد یا پاسخ هارت‌بیت را دریافت کند. در نتیجه وب‌ساکت با این خطا قطع می‌شود:

> `WS Connection lost: no close frame received or sent. Reconnecting in 5s...`

### ۳. باگ اصلی: ایجاد تسک‌های تکراری و موازی (Task Accumulation)

پس از ۵ ثانیه سیستم دوباره به سرور متصل می‌شود (`✅ Connected to CSMS`) و متد `_run_forever` مجدداً اجرا می‌شود. **اینجا دقیقاً همان جایی است که باگ رخ می‌دهد:**
به کدهای خط `634` نگاه کنید:

```python
if not self._hb_task or self._hb_task.done():
    self._hb_task = asyncio.create_task(self._heartbeat_loop())
else:
    logger.info("✅ Not Sending Heartbeat")

```

با اینکه سیستم لاگ انداخته `✅ Not Sending Heartbeat` (یعنی تسک هارت‌بیت قبلی هنوز زنده بوده و لغو نشده)، اما مشکل اینجاست که در بدنه هارت‌بیت قبلی (`_heartbeat_loop`)، یک متد قرار دارد به نام `_call_isregistered_api` که از کتابخانه کاملاً مسدودکننده (Synchronous/Blocking) یعنی **`requests`** استفاده می‌کند!

### ۴. فاجعه نهایی: مسدود شدن لوپ توسط `requests.get` (Read Timeout)

وقتی وب‌ساکت اول قطع شد، متد `_call_isregistered_api` همچنان در یک ترد (Thread) پس‌زمینه منتظر پاسخ سرور HTTP بود. پس از کانکت شدن مجدد وب‌ساکت، هارت‌بیت قبلی از خواب بیدار شده و دوباره لایو می‌شود.
از آنجا که این متد از `requests.get` با یک ترد اگزکوتور استفاده می‌کند، به دلیل مشکلات شبکه یا اورلود شدن لوپ، تردها مسدود می‌شوند و کل سیستم `asyncio` قفل (Freeze) می‌شود. این قفل شدن باعث می‌شود که هارت‌بیت بعدی ارور بدهد:

> `[HEARTBEAT] Failed to send Heartbeat:`

و در نهایت پس از ۱۰ ثانیه کل ساختار با خطای `ReadTimeout` متوقف می‌شود.

---

## 🛠️ چطور این باگ را برای همیشه حل کنیم؟

برای حل دائمی این مشکل، **دو اصلاح حیاتی** باید در کدهای مدیریت چرخه حیات (Lifecycle) انجام شود:

### اصلاح اول: تغییر کتابخانه `requests` به یک ابزار غیرمسدودکننده (Async)

استفاده از `requests` در کدهای `asyncio` (حتی با `to_thread`) گاهی اوقات باعث مسدود شدن کل لوپ در لایه شبکه می‌شود. بهترین کار این است که متدهای `_call_isregistered_api` و `_call_price_pr_kw` را با کتابخانه استاندارد **`aiohttp`** بنویسید تا کاملاً غیرمسدودکننده (Non-blocking) شوند.

اگر اصرار دارید کد دست نخورد، دست‌کم ساختار `_heartbeat_loop` را اصلاح کنید تا در صورت قطع شدن وب‌ساکت، تسک قبلی قطعاً کشته (`cancel`) شود.

### اصلاح دوم: مدیریت صحیح چرخه بازسازی اتصالات (Reconnection Cleanup)

هنگامی که خطای `WS Connection lost` رخ می‌دهد، شما باید تمام تسک‌های پس‌زمینه مربوط به اتصال قبلی (از جمله هارت‌بیت قدیمی) را کاملاً لغو کنید تا با اتصال جدید تداخل پیدا نکنند.

در متد `_run_forever` قبل از شروع بلوک `while` یا در بخش `except` قطع اتصال، تسک هارت‌بیت را به این صورت پاکسازی کنید:

```python
# در بخش مدیریت دیسکانکت متد _run_forever
except Exception as e:
    logger.error(f"WS Connection lost: {e}. Reconnecting in 5s...")
    # حتما تسک هارت بیت قبلی را لغو کنید تا با ابجکت وب ساکت جدید تداخل نکند
    if self._hb_task and not self._hb_task.done():
        self._hb_task.cancel()
    await asyncio.sleep(5)

```

### خلاصه دلیل اصلی:

ریشه اصلی باگ، **تداخل هارت‌بیت تسک قبلی (که به خاطر مسدود شدن `requests.get` در شبکه معلق مانده بود) با کانکشن وب‌ساکت جدید** بعد از دیسکانکشن ثانیه `00:34:10` است.
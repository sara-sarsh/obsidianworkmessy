ابزار `socat` (مخفف **SO**cket **CAT**) یکی از قدرتمندترین و منعطف‌ترین ابزارهای شبکه و ترمینال در لینوکس و مک است. اگر بخواهم خیلی ساده کارش را توصیف کنم: `socat` مثل یک **لوله‌کش** عمل می‌کند؛ دو نقطه (یا دو چیز) را به هم وصل می‌کند تا داده‌ها بتوانند از یکی وارد و از دیگری خارج شوند.

این "چیزها" می‌توانند فایل، پورت شبکه (TCP/UDP)، پورت سریال مجازی (PTY)، یا حتی خروجی یک دستور باشند.

در ادامه، پرکاربردترین سناریوهایی که در دنیای واقعی و برنامه‌نویسی به درد تان می‌خورند را با هم بررسی می‌کنیم.

---

### ۱. ساخت پورت سریال مجازی (همان کاری که برای تست کد انجام دادیم)

وقتی روی دستگاهت (مثل مک‌بوک) پورت سریال سخت‌افزاری نداری ولی کدت نیاز به یک پورت `/dev/tty` دارد، از این دستور استفاده می‌کنی:

```bash
socat -d -d pty,raw,echo=0 pty,raw,echo=0

```

**توضیح فلگ‌ها:**

* `-d -d`: مخفف double debug است. به `socat` می‌گوید مراحل کار و آدرس پورت‌هایی که ساخته را در ترمینال پرینت کند (بدون این فلگ، دستور در سکوت اجرا می‌شود).
* `pty`: مخفف Pseudo-Terminal است. یعنی یک پورت سریال فیک و نرم‌افزاری بساز.
* `raw`: به پایتون و سیستم‌عامل می‌گوید داده‌ها را دستکاری نکن و دقیقاً همان بایت‌های خام را رد و بدل کن.
* `echo=0`: اکو را غیرفعال می‌کند تا چیزی که می‌فرستی دوباره به سمت خودت برنگردد.

---

### ۲. فوروارد کردن یک پورت شبکه (Port Forwarding)

فرض کن یک سرور داری که دیتابیس یا یک وب‌سایت روی پورت `8080` آن بالا آمده، ولی می‌خواهی کاربران بتوانند از طریق پورت `80` به آن وصل شوند:

```bash
socat TCP-LISTEN:80,fork TCP:127.0.0.1:8080

```

**توضیح چیدمان:**

* `TCP-LISTEN:80`: به پورت 80 گوش بده (منتظر کانکشن بمان).
* `fork`: یک فلگ حیاتی است! باعث می‌شود `socat` بعد از وصل شدن اولین نفر بسته نشود. برای هر کانکشن جدید یک پردازش جدید می‌سازد تا چندین نفر هم‌زمان بتوانند وصل شوند.
* `TCP:127.0.0.1:8080`: هر کسی به پورت 80 وصل شد، ترافیکش را بفرست به پورت 8080.

---

### ۳. تبدیل پورت سریال به شبکه (Serial to Network)

این سناریو در اینترنت اشیاء (IoT) و سخت‌افزار خیلی رایج است. فرض کن یک دستگاه داری که به پورت سریال سرورت وصل است و می‌خواهی از طریق شبکه و با پروتکل TCP به دیتای آن دسترسی داشته باشی:

```bash
socat TCP-LISTEN:9000,fork /dev/ttyUSB0,raw,echo=0,baud=115200

```

با این دستور، هر وقت با تلنت یا کد پایتون به پورت `9000` شبکه وصل شوی، انگار مستقیم به کابل سریال دستگاه متصل شده‌ای.

---

### ۴. گوش دادن به یک پورت و دیدن دیتا (شبیه به Netcat)

اگر می‌خواهی یک پورت TCP باز کنی و هر دیتایی که دیگران به آن می‌فرستند را در ترمینال ببینی (مثلاً برای تست وب‌هوکرها یا فرستنده‌ها):

```bash
socat TCP-LISTEN:7777,crlf STDOUT

```

* `STDOUT`: یعنی خروجی استاندارد ترمینال. به زبان ساده: هر چیزی از پورت 7777 آمد را روی صفحه نمایش بده.
* `crlf`: کاراکترهای پایان خط (`\r\n`) را تصحیح می‌کند تا متن‌ها درست چاپ شوند.

---

### ۵. فرستادن فایل از طریق شبکه

اگر روی دو تا سیستم `socat` داشته باشی، می‌توانی خیلی سریع فایل جابجا کنی.

* **روی سیستم مقصد (گیرنده):**
```bash
socat TCP-LISTEN:5555 OPEN:received_file.txt,create

```


*(منتظر می‌ماند تا کسی وصل شود و دیتای ورودی را داخل یک فایل جدید ذخیره کند).*
* **روی سیستم مبدا (فرستنده):**

```bash
  socat OPEN:my_file.txt TCP:192.168.1.50:5555

```

*(فایل را باز می‌کند و می‌فرستد به آی‌پی سیستم مقصد).*

---

### 💡 فرمول طلایی `socat` برای همیشه

هر وقت خواستی یک دستور `socat` بنویسی، این ساختار ساده را در ذهنت مرور کن:

```bash
socat [فلگ‌های عمومی]  <آدرس منبع و تنظیماتش>  <آدرس مقصد و تنظیماتش>

```

این ابزار هیچ محدودیتی ندارد؛ حتی می‌توانی دو تا پورت شبکه مختلف را به هم وصل کنی یا خروجی یک اسکریپت باش را مستقیم بفرستی روی یک پورت سریال! همین انعطاف‌پذیری باعث شده که در کارهای دوآپس (DevOps) و تست سیستم‌های نهفته (Embedded) به یک آچار فرانسه تبدیل شود.

```

```
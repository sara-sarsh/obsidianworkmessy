## opi_hardware

### list of publish topic:
keys:
	[0 -11]
	e.g :
		broker.publish (keys, 2)
id_token
	["s.th"]
	e.g :
		broker.publish (id_token/gun_x, "s.th")
	
### list of subscribe topic:
None---
## lpc_interface
### list of publish topic:
id_token
["s.th"]
	e.g :
		broker.publish (id_token/gun_x, "s.th")
gun_status
	[0 - x]
	broker.publish(gun_status, 5)
all charging metrics (each in seperate topic: gun_1/vlotage and ...)
	broker.publish(voltage/gun_x, 22.5)
	_charge_metrics = [
        "voltage", "current", "charge_percentage", "battary_voltage",
        "demand_current", "charge_time", "used_energy", 
	     "charge_mode", "charge_limiter",
        "insulator_resistor", "pole_temp_positive", "pole_temp_negetive",
        "each_cell_voltage", "dual_power",
    ]
	
stoped(by_ev)
	by_ev,  by_server, by_nfc
	broker.publish(stop/gun_x, "by_ev")
error
	[0 - x]
	broker.publish(error/gun_x, 5)
transactionStopRequested
	true, false
	broker.publish(transactionStopRequested/gun_x, true)### list of 
	
### subscribe topic:
start
stoping (by_server)
stoping (by_nfc)
keys

## ocpp
### list of publish topic:
auth_resp
	Accepted, Rejected
	broker.publish(auth_resp/gun_x, "Accepted")
start
	by_nfc, by_server, by_vin
	broker.publish(start/gun_x, "by_x")
stop (by_server)
	by_ev,  by_server, by_nfc
	broker.publish(stop/gun_x, "by_server")
stop(by_nfc)
	by_ev,  by_server, by_nfc
	broker.publish(stop/gun_x, "by_nfc")
price_per_kw
	36343
	broker.publish(price_per_kw/gun_x, 36343)
### list of subscribe topic:

key
stop_by_ev
id_token

transactionStopRequested
	true, false
	broker.publish(transactionStopRequested/gun_x, true)

## gui
### list of publish topic:
total_price
	36343
	broker.publish(total_price/gun_x, 36343)
	

### list of subscribe topic:
keys
id_token
gun_status
error
auth_resp
all charging metrics (each in seperate topic: gun_1/vlotage and ...)
stop(by_ev)
stop(by_server)

sudo apt-get update
sudo apt-get install docker-compose-plugin

برای پیدا کردن دلیل این مشکل، باید دقیقاً مثل یک کارآگاه عمل کنیم و ببینیم آیا فایل‌ها اصلاً وارد دنیای داکر شده‌اند یا نه. دلیل اینکه `ConfigManager` مقدار `UNKNOWN` برمی‌گرداند این است که فایل `/etc/evck/device.json` را پیدا نمی‌کند یا نمی‌تواند بخواند.

برای دیباگ کردن این مشکل، این ۳ قدم را با هم انجام می‌دهیم:

### قدم اول: نفوذ به داخل کانتینر (بررسی فایل‌های داخل داکر)

ما می‌توانیم یک ترمینال (Shell) مستقیماً داخل کانتینرِ در حال اجرا باز کنیم و ببینیم داکر چه فایل‌هایی را می‌بیند. در ترمینال اورنج‌پای دستور زیر را بزن:

```bash
docker exec -it evck_ocpp bash

```

*(اگر ارور داد به جای `bash` کلمه `sh` را بنویس).*

حالا شما داخل کانتینر هستید! دستورات زیر را بزن تا ببینیم فایل‌ها وجود دارند یا نه:

```bash
ls -la /etc/evck/
ls -la /var/lib/evck/
cat /etc/evck/device.json

```

* **اگر ارور `No such file or directory` داد:** یعنی Volume Mapping شما در فایل `docker-compose.yml` اشتباه است و کانتینر اصلاً به فایل‌های اورنج‌پای وصل نیست.
* **برای خروج از کانتینر:** تایپ کن `exit` و اینتر بزن.

---

### قدم دوم: بررسی فایل‌ها روی خود اورنج‌پای (Host)

آیا مطمئنی که فایل‌ها اصلاً روی خود لینوکس اورنج‌پای ساخته شده‌اند؟
یادت باشد که `ProvisioningScript.py` فایل‌ها را در مسیر مطلق `/etc/evck` و `/var/lib/evck` می‌سازد. روی ترمینال اورنج‌پای (خارج از داکر) چک کن:

```bash
cat /etc/evck/device.json

```

**نکته بسیار مهم:** اگر روی خود اورنج‌پای این فایل وجود ندارد، یعنی شما هنوز اسکریپت Provision را برای بار اول اجرا نکرده‌اید!
قبل از اینکه سرویس OCPP را استارت بزنی، باید یک بار دستی `ProvisioningScript.py` را اجرا کنی تا فایل‌های `device.json` و `config.json` از روی `master_config.json` ساخته شوند.

---

### قدم سوم: اصلاح Volume ها در `docker-compose.yml`

اگر فایل‌ها روی اورنج‌پای هستند ولی داخل داکر نیستند، مشکل قطعا از فایل کامپوز است.
با توجه به اینکه پروویژن شما فایل‌ها را در مسیر لینوکسی `/etc/evck/` ذخیره می‌کند، فایل `docker-compose.yml` شما **باید** این مپینگ‌ها را داشته باشد:

```yaml
      volumes:
        - ./:/opt/evck  # کدهای پروژه
        - /etc/evck:/etc/evck          # وصل کردن هویت دستگاه به داکر
        - /var/lib/evck:/var/lib/evck  # وصل کردن کانفیگ‌های پویا به داکر
        - /etc/installertools:/etc/installertools # دسترسی به فایل مستر و پروویژن

```

**چرا این مپینگ‌ها واجب‌اند؟**
پایتون داخل داکر می‌گوید: "من `/etc/evck/device.json` را می‌خواهم". داکر به Volume ها نگاه می‌کند و می‌گوید: "آها، کاربر به من گفته پوشه `/etc/evck` اورنج‌پای را بگذارم جای پوشه `/etc/evck` خودم". اینطوری فایل خوانده می‌شود.

### خلاصه کاری که الان باید بکنی:

1. روی اورنج‌پای چک کن ببین `device.json` در مسیر `/etc/evck/` وجود دارد یا نه.
2. اگر نبود، یک بار اسکریپت پروویژن را اجرا کن تا ساخته شود.
3. فایل `docker-compose.yml` را مثل الگو بالا اصلاح کن تا پوشه‌های `/etc/evck` و `/var/lib/evck` حتماً به داخل داکر مپ شوند.
4. داکر را ریلود کن: `docker compose down` و بعد `docker compose up -d --build`.
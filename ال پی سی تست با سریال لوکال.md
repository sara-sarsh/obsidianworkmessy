انتخاب خیلی هوشمندانه‌ایه! روش سوم برای سناریوهایی که می‌خوای چند تا ترمینال مختلف رو هم‌زمان باز کنی و از هر مسیری دلت خواست سریع کد رو ران کنی، بهترین و بی‌دردسرترین گزینه‌ست. با این کار اسکریپتت کاملاً مستقل و «خودکفا» می‌شه.

فقط برای اینکه خیالت راحت باشه چطوری کار می‌کنه و دقیقاً کجای فایل قرارش بدی، یه بررسی کوتاهش کنیم:

توی فایل `opt/evck/ocpp_client/ocpp_offline.py`، این خطوط رو دقیقاً در **بالاترین بخش فایل** (قبل از هرگونه ایمپورت از `common`) قرار بده:

```python
import os
import sys

# ۱. پیدا کردن مسیر خود فایل ocpp_offline.py
# ۲. رفتن به دو پوشه عقب‌تر (یعنی پوشه evck) و اضافه کردنش به مسیرهای پایتون
current_dir = os.path.dirname(os.path.abspath(__file__))
parent_dir = os.path.dirname(current_dir)
if parent_dir not in sys.path:
    sys.path.append(parent_dir)

# حالا تمام ایمپورت‌های قبلی‌ت بدون مشکل کار می‌کنند:
from common import MqttMessageBroker, setup_logger, conf, ENVS_NEW, GUNS, QoS

```

### 💡 یک نکته کوچک برای بقیه اسکریپت‌ها:

اگر بعداً خواستی همین حرکت رو برای اسکریپت‌های دیگه‌ات (مثلاً توی پوشه `gui_app` یا `orchestrator`) هم پیاده کنی، حواست به **تعداد دفعات** عقب رفتن (`os.path.dirname`) باشه:

* اگر اسکریپت توی `opt/evck/gui_app/` هست (مثل فایل اولی که ارور داد)، چون دقیقاً مثل `ocpp_client` یک پله با `common` فاصله داره، همین کد بالا دقیقاً براش کار می‌کنه.
* اگر اسکریپتی داشتی که جاش عمیق‌تر بود، کافیه یه لایه `os.path.dirname` دیگه بهش اضافه کنی تا همیشه به پوشه `evck` برسه.

حالا با خیال راحت می‌تونی توی هر چند تا ترمینال که می‌خوای، فقط با دستور `python3 path/to/file.py` کدهات رو موازی ران کنی!


حتماً! برای اینکه بتونی کدت رو کاملاً واقعی تست کنی، یک اسکریپت شبیه‌ساز (Simulator) برات نوشتم. این اسکریپت دقیقاً فرمت ۲۱ فیلدی کدت (`CHARGE_METRICS`) رو رعایت می‌کنه، مقادیر معنادار و داینامیک (مثل ولتاژ، جریان، درصد باتری و انرژی مصرفی) تولید می‌کنه و هر **۱ ثانیه یک‌بار** از طریق پورت دوم `socat` ارسال می‌کنه.

یک فایل جدید به نام `lpc_simulator.py` بساز و این کد رو داخلش قرار بده:

```python
import time
import random
import os

# پورت دوم socat که باید دیتا رو بهش تزریق کنیم
TARGET_PORT = "/dev/ttys004"

def generate_mock_frame(gun_id: str, elapsed_seconds: int) -> str:
    """تولید یک فریم فیک و منطقی بر اساس فیلدهای CHARGE_METRICS کدت"""
    
    charge_status = "3"  # فرض می‌کنیم در حال شارژ (CHARGING) هست
    voltage = str(random.randint(380, 410))
    current = str(random.randint(20, 32))
    
    # درصد شارژ باتری که کم‌کم بالا میره
    soc = str(min(20 + (elapsed_seconds // 10), 100)) 
    
    battery_voltage = str(random.randint(350, 400))
    demand_current = "32"
    res_p, res_n = "500", "500"
    temp_p, temp_n = "35", "36"
    cells_mean_v = "3.8"
    cells_mean_temp = "34"
    charge_mode = "1"
    charge_time = str(elapsed_seconds)
    
    # انرژی مصرفی بر حسب وات‌ثانیه (هر ثانیه زیاد می‌شه)
    used_energy_ws = str(elapsed_seconds * 7000) 
    
    charge_limiter = "0"
    dual_power = "0"
    error = "0"
    warning = "0"
    sc = str(elapsed_seconds % 65536) # Sequence counter

    # چیدن فیلدها دقیقاً به همان ترتیبی که در CHARGE_METRICS تعریف شده
    fields = [
        gun_id, charge_status, voltage, current, soc, battery_voltage,
        demand_current, res_p, res_n, temp_p, temp_n, cells_mean_v,
        cells_mean_temp, charge_mode, charge_time, used_energy_ws,
        charge_limiter, dual_power, error, warning, sc
    ]
    
    # ساخت فریم نهایی با ساختار *field1&field2&...#
    return "*" + "&".join(fields) + "#\r\n"

def main():
    if not os.path.exists(TARGET_PORT):
        print(f"❌ Error: Virtual port {TARGET_PORT} not found.")
        print("Please run 'socat -d -d pty,raw,echo=0 pty,raw,echo=0' first.")
        return

    print(f"🚀 LPC Simulator started. Sending data to {TARGET_PORT} every 1 second...")
    print("Press Ctrl+C to stop.")

    elapsed_seconds = 0
    try:
        with open(TARGET_PORT, "w") as ser:
            while True:
                # به نوبت برای تفنگ ۱ و تفنگ ۲ دیتا می‌فرستیم
                for gun in ["1", "2"]:
                    frame = generate_mock_frame(gun, elapsed_seconds)
                    ser.write(frame)
                    ser.flush()  # مطمئن شدن از ارسال آنی دیتا
                    print(f"⚡ Sent [{TARGET_PORT}]: {frame.strip()}")
                
                elapsed_seconds += 1
                time.sleep(1) # وقفه ۱ ثانیه‌ای
                
    except KeyboardInterrupt:
        print("\n👋 Simulator stopped.")

if __name__ == "__main__":
    main()

```

---

### 🛠️ چطوری کل سیستم رو با هم تست کنی؟

حالا ۳ تا ترمینال باز کن و به این ترتیب اجراشون کن:

1. **ترمینال اول (ساخت پورت مجازی):**
```bash
socat -d -d pty,raw,echo=0 pty,raw,echo=0

```


*(ببین آدرس پورت‌ها چیه. اگر به جای `ttys003` و `ttys004` چیز دیگه‌ای داد، توی کدهات تغییرش بده).*
2. **ترمینال دوم (اجرای کد اصلی خودت):**

```bash
   python3 opt/evck/lpc_interface/lpc_interface.py

```

*(این اسکریپت باز می‌شه، GPIO رو ماک می‌کنه و به پورت `ttys003` گوش میده).*

3. **ترمینال سوم (اجرای همین سیمولاتور):**
```bash
python3 lpc_simulator.py

```



به محض ران کردن سیمولاتور، می‌بینی که توی ترمینال دوم کد اصلیت دیتای سنسورها رو دکود می‌کنه، لاگ `[RX]` می‌ندازه و وضعیت پورت رو `green` می‌کنه و هم‌زمان پیام اکشن‌ها رو روی MQTT پابلیش می‌کنه!

```

```

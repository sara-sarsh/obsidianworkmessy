# EVCK160C Git Workflow and Repository Rules

این سند قوانین و روند استاندارد استفاده از Git برای پروژه EVCK160C را مشخص می‌کند.

---

## 1. هدف

هدف این workflow این است که:

- کد پایدار همیشه روی `main` باشد.
- توسعه قابلیت‌های جدید در branch جدا انجام شود.
- releaseها با tag مشخص شوند.
- GitHub به عنوان remote اصلی استفاده شود.
- سرور داخلی/Gitea به عنوان mirror یا remote داخلی استفاده شود.
- از ایجاد branchهای پراکنده و نامفهوم جلوگیری شود.

---

## 2. ساختار Remoteها

ساختار استاندارد remoteها باید به شکل زیر باشد:

origin      = GitHub repository
internal    = Gitea/internal organization Git server
backup      = optional local/NAS backup repository


مثال:

bash
git remote -v

خروجی مطلوب:

origin    git@github-work:farhad3113teslapower/evck160c.git
internal  ssh://git@gitea-server/org/evck160c.git

---

## 3. Branchهای اصلی

### `main`

Branch اصلی و پایدار پروژه.

قوانین:

- همیشه باید قابل build و deploy باشد.
- فقط کد تست‌شده وارد آن شود.
- release tagها فقط از روی `main` ساخته شوند.
- commit مستقیم روی `main` ممنوع یا حداقل محدود باشد.

### `develop` - اختیاری

اگر پروژه به مرحله‌ای رسید که چند feature همزمان توسط چند نفر توسعه داده می‌شود، می‌توان branch `develop` داشت.

در حال حاضر اگر تیم کوچک است، استفاده از `main + feature branches` کافی است.

---

## 4. نام‌گذاری Branchها

از این به بعد branchها باید با یکی از prefixهای زیر ساخته شوند:

feature/...
fix/...
hotfix/...
release/...
test/...
docs/...
backup/...
tools/...

مثال‌های خوب:

feature/kiosk-layout
feature/ocpp-offline-mode
fix/mqtt-reconnect
fix/lpc-timeout
hotfix/update-crash
release/v2.1.0
test/autoupdate
docs/install-guide

از نام‌های نامنظم مانند موارد زیر اجتناب شود:

text
making-gui
making-lpc
making-ENvs
makeing-decentrelized

این branchها قدیمی هستند و به مرور باید merge/archive/delete شوند.

---

## 5. شروع کار روی یک feature جدید

همیشه از آخرین نسخه `main` شروع کن:


```bash
git switch main
git pull origin main
git switch -c feature/name-of-feature
```


مثال:

```bash
git switch main
git pull origin main
git switch -c feature/kiosk-responsive-ui
```

---

## 6. commit کردن

commitها باید کوچک، واضح و قابل فهم باشند.

فرمت پیشنهادی:

type: short description

typeهای مجاز:

text
feat      قابلیت جدید
fix       رفع باگ
refactor  بازسازی کد بدون تغییر رفتار
docs      تغییر مستندات
style     تغییر ظاهری/فرمت
test      اضافه یا اصلاح تست
chore     کارهای جانبی، تنظیمات، dependency
build     تغییرات build/deploy

مثال:

bash
git commit -m "feat: add kiosk responsive layout"
git commit -m "fix: handle mqtt reconnect failure"
git commit -m "docs: update installation guide"

---

## 7. push کردن branch جدید

برای اولین push یک branch:

bash
git push -u origin feature/name-of-feature

بعد از آن کافی است:

bash
git push

---

## 8. merge کردن feature به main

وقتی feature کامل و تست شد:

bash
git switch main
git pull origin main
git merge --no-ff feature/name-of-feature
git push origin main

بعد از merge موفق، branch می‌تواند حذف شود:

bash
git branch -d feature/name-of-feature
git push origin --delete feature/name-of-feature

---

## 9. Pull Request

اگر روی GitHub یا Gitea کار می‌کنیم، روش ترجیحی این است:

1. feature branch ساخته شود.
2. تغییرات push شود.
3. Pull Request از feature branch به `main` ساخته شود.
4. تست‌ها بررسی شوند.
5. تغییرات review شوند.
6. سپس merge شود.

مثال:

text
feature/kiosk-ui -> main

مزایا:

- review
- history تمیزتر
- امکان اجرای CI
- جلوگیری از خراب شدن `main`

---

## 10. کار با تغییرات نیمه‌کاره

اگر وسط کار روی یک branch هستیم و نیاز داریم سراغ کار دیگری برویم:

### حالت اول: تغییرات فعلی را می‌خواهیم نگه داریم

بهتر است commit موقت بزنیم:

bash
git add .
git commit -m "wip: temporary checkpoint"

سپس branch جدید بسازیم:

bash
git switch -c feature/other-work

بعدا commitهای WIP باید تمیز شوند یا با commit مناسب جایگزین شوند.

### حالت دوم: نمی‌خواهیم commit کنیم

می‌توانیم از stash استفاده کنیم:

bash
git stash push -m "wip: current gui work"
git switch main
git switch -c feature/other-work
git stash pop

ولی برای پروژه‌های بزرگ، commit موقت معمولا امن‌تر از stash است.

---

## 11. Rebase و Merge

### Merge

برای آوردن تغییرات یک branch داخل branch دیگر:

bash
git switch target-branch
git merge source-branch

مثال:

bash
git switch main
git merge feature/kiosk-ui

### Rebase

برای قرار دادن commitهای branch فعلی روی آخرین نسخه branch دیگر:

bash
git switch feature/my-work
git rebase main

Rebase بیشتر برای تمیز کردن history استفاده می‌شود.

قانون مهم:

> روی branchهایی که shared/public شده‌اند و دیگران ممکن است از آن استفاده کنند، rebase نکن مگر با هماهنگی.

---

## 12. Versioning

پروژه از Semantic Versioning استفاده می‌کند:

text
MAJOR.MINOR.PATCH

مثال:

text
v2.0.0
v2.0.1
v2.1.0
v3.0.0

معنی:

text
MAJOR  تغییرات ناسازگار یا breaking
MINOR  قابلیت جدید بدون breaking change
PATCH  رفع باگ

---

## 13. ساخت release

Release فقط باید از روی `main` ساخته شود.

bash
git switch main
git pull origin main
git tag -a v2.0.1 -m "Release v2.0.1"
git push origin v2.0.1

اگر remote داخلی داریم:

bash
git push internal v2.0.1

---

## 14. قوانین tag

- tag را برای هر commit کوچک نسازید.
- tag فقط برای release واقعی ساخته شود.
- tagهای beta/rc مجاز هستند:

text
v2.1.0-beta.1
v2.1.0-rc.1

- tag نباید بدون دلیل جابه‌جا شود.
- اگر tag اشتباه ساخته شد، قبل از حذف یا تغییر آن باید با تیم هماهنگ شود.

---

## 15. حذف branchهای قدیمی

قبل از حذف branch، چک کن که merge شده باشد:

bash
git branch --merged main

اگر branch داخل لیست بود، می‌توان حذف کرد:

bash
git branch -d branch-name

برای حذف از remote:

bash
git push origin --delete branch-name

برای branchهای merge نشده:

bash
git branch --no-merged main

این branchها نباید بدون بررسی حذف شوند.

---

## 16. Backup

قبل از تغییرات مهم روی remoteها یا پاکسازی بزرگ:

bash
git bundle create full-backup.bundle --all

این فایل شامل کل history، branchها و tagها است.

---

## 17. وضعیت مطلوب روزانه

قبل از شروع کار:

bash
git status
git switch main
git pull origin main

برای شروع feature:

bash
git switch -c feature/my-feature

بعد از کار:

bash
git status
git add .
git commit -m "feat: meaningful message"
git push -u origin feature/my-feature

---

## 18. قانون طلایی

- `main` همیشه باید تمیز و قابل اعتماد باشد.
- کار نیمه‌کاره روی `main` انجام نشود.
- branchهای قدیمی باید یا merge شوند، یا archive، یا delete.
- GitHub remote اصلی است.
- Gitea/internal remote برای سازمان، mirror و backup استفاده می‌شود.


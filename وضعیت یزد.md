بله، از روی همین لاگ‌ها می‌شود یک جمع‌بندی نسبتاً مطمئن داد.

1) گزارش وضعیت فعلی و علت احتمالی خاموش شدن مانیتور
خلاصه خیلی کوتاه
در لحظه‌ای که این لاگ‌ها گرفته شده‌اند، HDMI قطع نیست و Xorg و Chromium هم بالا هستند.

پس خاموش شدن مانیتور به‌احتمال زیاد به خاطر disable شدن HDMI توسط سیستم نیست.

اما یک نکته خیلی مهم داریم:

دمای GPU/CPU/DDR حدود 84 تا 86 درجه است
این دما بالاست
با اینکه لاگ throttle/thermal صریحی دیده نمی‌شود، این دما کاملاً می‌تواند باعث ناپایداری تصویری، هنگ لحظه‌ای، سیاه شدن موقت تصویر، یا رفتار غیرعادی GPU/Chromium/Xorg شود
پس نتیجه عملی:

نتیجه نهایی
دلیل اصلیِ محتمل برای خاموش/سیاه شدن مانیتور، بیشتر ناپایداری ناشی از دمای بالا است، نه قطع HDMI و نه down شدن سرویس‌های GUI.

تحلیل دقیق لاگ‌ها
الف) HDMI سالم و متصل است
این خروجی خیلی مهم است:

bash
/sys/class/drm/card0-HDMI-A-1/status: connected
یعنی از دید kernel/DRM، نمایشگر واقعاً وصل است.

همچنین modes هم خوانده شده:

bash
1920x1080
1280x720
...
این یعنی EDID مانیتور خوانده شده و کارت گرافیک رزولوشن‌های مانیتور را می‌شناسد.

پس این سناریوها عملاً رد می‌شوند:

کابل HDMI قطع باشد
مانیتور از دید سیستم شناسایی نشده باشد
خروجی HDMI از ریشه dead باشد
ب) Xorg سالم است
این دستور موفق بوده:

bash
DISPLAY=:0 xset -q
پس X server زنده است.

همچنین:

bash
/usr/lib/xorg/Xorg -nolisten tcp :0 ...
پس Xorg واقعاً در حال اجراست.

یعنی اگر مانیتور سیاه می‌شود، در زمان گرفتن این لاگ‌ها Xorg نخوابیده و کرش هم نکرده.

ج) Chromium kiosk هم بالا است
از این بخش:

bash
evck-kiosk.service ... Active: active (running)
و این‌ها:

bash
chrome --app=http://127.0.0.1:8082 --start-fullscreen ...
chrome --type=gpu-process ...
chrome --type=renderer ...
openbox
مشخص است که:

startx اجرا شده
openbox بالا است
chromium بالا است
GPU process هم فعال است
پس در زمان لاگ‌گیری، کلاً استک نمایش زنده است.

د) GPU rendering هم کار می‌کند
این خروجی مهم است:

bash
OpenGL renderer string: Mali-G31 (Panfrost)
OpenGL version string: 3.1 Mesa 23.2.1...
یعنی:

درایور گرافیکی کار می‌کند
رندر GPU فعال است
OpenGL stack زنده است
پس مشکل از این نیست که GPU اصلاً بالا نیامده باشد.

هـ) رزولوشن و چرخش تصویر درست اعمال شده
از xrandr:

bash
current 1080 x 1920
HDMI-1 connected primary 1080x1920+0+0 left
یعنی:

مانیتور فیزیکی 1920x1080 است
تصویر rotate شده و به حالت portrait درآمده
این با نصب EVCK و rotation سازگار است
پس خروجی تصویر از نظر mode-setting هم برقرار است.

و) هیچ خطای واضح HDMI/DRM در dmesg دیده نشده
این‌ها خروجی خالی داده‌اند:

bash
dmesg -T | grep -Ei 'hdmi|drm|display|edid|gpu'
journalctl -b | grep -Ei 'Xorg|hdmi|drm|modeset'
dmesg -T | grep -Ei 'thermal|thrott|overheat'
این یعنی:

فعلاً خطای واضح kernel-level ثبت نشده
قطع و وصل HDMI یا crash آشکار DRM در لاگ نیست
این به نفع این فرض است که مشکل intermittent است یا هنوز در لاگ capture نشده.

مهم‌ترین یافته: دما خیلی بالاست
این بخش از همه مهم‌تر است:

bash
gpu-thermal  84.4C
ve-thermal   84.5C
cpu-thermal  85.5C
ddr-thermal  83.8C
و بار دوم:

bash
gpu-thermal  84.2C
ve-thermal   84.0C
cpu-thermal  84.9C
ddr-thermal  83.4C
تفسیر
برای Orange Pi Zero 2W این دماها:

بحرانی مطلق نیستند
اما کاملاً بالا و نگران‌کننده هستند
مخصوصاً برای کار 24/7 در باکس/کیوسک
وقتی CPU و GPU مدام روی 85°C هستند، این اتفاق‌ها محتمل می‌شود:

افت فرکانس مقطعی
لگ در رندر Chromium
فریز کوتاه GPU process
black frame یا سیاه شدن موقت
ناپایداری بیشتر در شتاب‌دهی GPU
بدتر شدن وضعیت اگر تهویه ضعیف باشد
نکته مهم‌تر:

bash
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq
1512000
...
1200000
این پایین آمدن فرکانس می‌تواند طبیعی باشد، ولی در کنار دمای 85 درجه، احتمال throttle ضمنی را بالا می‌برد؛ حتی اگر لاگ صریح thermal/throttle ثبت نشده باشد.

آیا سیستم HDMI را به خاطر گرما خاموش می‌کند؟
از روی این لاگ‌ها: نه، شواهدی نداریم که HDMI disable شده باشد.

چون:

HDMI connected است
modes دارد
xrandr آن را connected نشان می‌دهد
Xorg زنده است
Chromium زنده است
GPU renderer فعال است
پس اگر اپراتور می‌گوید «مانیتور خاموش می‌شود»، محتمل‌ترین سناریوها این‌ها هستند:

سناریوهای محتمل‌تر
1) مانیتور تصویر نمی‌گیرد ولی لینک HDMI هنوز برقرار است
یعنی:

از دید لینوکس همه‌چیز connected است
ولی پنل تصویر سیاه نشان می‌دهد
علت می‌تواند:

ناپایداری گرمایی
باگ GPU/Chromium
مشکل خود مانیتور در دمای محیط
کیفیت کابل/نویز تغذیه
2) Chromium یا GPU process لحظه‌ای freeze می‌کند
هرچند الان بالا هستند، ولی ممکن است intermittent باشد.

3) مشکل برق یا افت ولتاژ
گاهی روی بردهای SBC، مشکل ظاهراً “display” است ولی در اصل:

آداپتور ضعیف است
کابل برق افت دارد
تحت بار و گرما، GPU/SoC ناپایدار می‌شود
این مورد را هم جدی بگیرید، مخصوصاً اگر:

هم‌زمان شبکه هم ناپایدار شده
یا USB/other peripherals دارید
4) مانیتور خودش sleep / reset می‌کند
هرچند DPMS در X غیرفعال است:

bash
DPMS is Disabled
timeout: 0
این عالی است، پس X خودش صفحه را خاموش نمی‌کند.

اما بعضی مانیتورها اگر:

سیگنال برای لحظه‌ای ناپایدار شود
فرکانس/mode عجیب ببینند
برق خودشان نوسان داشته باشد
خودشان black screen می‌دهند.

یک نکته فرعی ولی مهم: مشکل شبکه هم دارید
این بخش:

bash
NETWORK DOWN
internet_connected: False
server_reachable: False
ocpp_websocket_connected: True
Ping 8.8.8.8 failed
و شما هم تایید کردی:

bash
ping 1.1.1.1 -> 100% packet loss
ping 8.8.8.8 -> 100% packet loss
یعنی دستگاه اینترنت عمومی ندارد.

این مستقیم دلیل خاموشی مانیتور نیست، ولی ممکن است باعث شود UI در بعضی سناریوها:

stuck شود
loading بماند
صفحه سفید/سیاه بدهد اگر front-end به backend/network وابسته باشد
اما با توجه به اینکه GUI RX لاگ می‌دهد و Chromium هم اجراست، در این کیس، شبکه مشکل اصلی خاموشی مانیتور به نظر نمی‌رسد.

جمع‌بندی فنی نهایی
چیزهایی که رد می‌شوند
HDMI cable disconnect
مانیتور از دید سیستم شناخته نشده
Xorg down
Chromium down
GPU driver اصلاً لود نشده
DPMS/X screen blanking
چیزهایی که محتمل‌اند
دمای بالا و ناپایداری حرارتی
GPU/Chromium freeze مقطعی
مشکل پاور یا افت ولتاژ
مشکل خود مانیتور/کابل در شرایط گرمایی
باگ intermittent که در لحظه لاگ‌گیری رخ نداده
پیشنهاد عملی من برای تشخیص قطعی‌تر
اولویت 1: دما را پایین بیاورید
اگر هیت‌سینک/فن ندارید، اضافه کنید.

هدف خوب برای کار پایدار:

CPU زیر 75°C
بهتر: زیر 70°C
اگر بعد از خنک‌سازی مشکل مانیتور از بین رفت، تقریباً علت روشن می‌شود.

اولویت 2: تست بدون GPU acceleration
شما Chromium را با این فلگ‌ها بالا آورده‌اید:

--enable-gpu-rasterization
--enable-zero-copy
--use-gl=angle
--use-angle=gl
--enable-native-gpu-memory-buffers
برای تشخیص، یک بار موقت این‌ها را حذف کنید یا حتی این را اضافه کنید:

bash
--disable-gpu
اگر black screen قطع شد، مشکل از مسیر GPU acceleration است.

اولویت 3: لاگ‌برداری دوره‌ای
باید وقتی مانیتور “خاموش” می‌شود، همان لحظه این‌ها ثبت شوند:

HDMI status
xrandr
xset
temp
cpu freq
screenshot
Chromium/Xorg status
DRM/journal tail
2) اسکریپت کامل و جامع health check
پایین یک اسکریپت کامل می‌دهم که:

HDMI را چک می‌کند
Xorg/Openbox/Chromium را چک می‌کند
سرویس‌های EVCK را چک می‌کند
دماها را می‌خواند
فرکانس CPU را می‌گیرد
حافظه/دیسک/load را می‌گیرد
شبکه را چک می‌کند
OpenGL را چک می‌کند
لاگ‌های مهم را tail می‌کند
در صورت وجود، screenshot هم می‌گیرد
خروجی هم human-readable و هم JSON-like ساده در فایل می‌دهد
اسکریپت: /opt/evck/tools/evck-healthcheck.sh
bash
#!/usr/bin/env bash
set -u

OUT_DIR="${1:-/opt/evck/logs/healthchecks}"
TS="$(date '+%Y%m%d-%H%M%S')"
HOST="$(hostname)"
REPORT="$OUT_DIR/health-$HOST-$TS.txt"
JSON="$OUT_DIR/health-$HOST-$TS.json"
SCREENSHOT="$OUT_DIR/screenshot-$HOST-$TS.png"

mkdir -p "$OUT_DIR"

have_cmd() {
  command -v "$1" >/dev/null 2>&1
}

kv_json() {
  local k="$1"
  local v="$2"
  v="${v//\\/\\\\}"
  v="${v//\"/\\\"}"
  printf '  "%s": "%s"' "$k" "$v"
}

section() {
  echo
  echo "===== $1 ====="
}

append_json_pair() {
  local key="$1"
  local val="$2"
  if [ "${JSON_FIRST:-1}" -eq 1 ]; then
    kv_json "$key" "$val" >> "$JSON"
    JSON_FIRST=0
  else
    printf ',\n' >> "$JSON"
    kv_json "$key" "$val" >> "$JSON"
  fi
}

{
  echo "EVCK Health Check Report"
  echo "Timestamp: $(date '+%F %T %Z')"
  echo "Host: $HOST"
  echo "Kernel: $(uname -a)"
  echo "Uptime: $(uptime -p 2>/dev/null || uptime)"
} > "$REPORT"

echo "{" > "$JSON"
JSON_FIRST=1

# Basic system info
append_json_pair "timestamp" "$(date '+%F %T %Z')"
append_json_pair "hostname" "$HOST"
append_json_pair "kernel" "$(uname -a)"
append_json_pair "uptime" "$(uptime -p 2>/dev/null || uptime)"

section "OS / UPTIME" >> "$REPORT"
{
  echo "Hostname: $HOST"
  echo "Date: $(date '+%F %T %Z')"
  echo "Kernel: $(uname -a)"
  echo "Uptime: $(uptime -p 2>/dev/null || uptime)"
  echo "Load: $(cat /proc/loadavg)"
} >> "$REPORT"
append_json_pair "loadavg" "$(cat /proc/loadavg)"

# HDMI / DRM
section "HDMI / DRM" >> "$REPORT"
HDMI_STATUS_SUMMARY=""
HDMI_MODES_SUMMARY=""
for f in /sys/class/drm/*HDMI*/status; do
  [ -e "$f" ] || continue
  val="$(cat "$f" 2>/dev/null)"
  echo "$f: $val" >> "$REPORT"
  HDMI_STATUS_SUMMARY="${HDMI_STATUS_SUMMARY}${f}=${val}; "
done
for f in /sys/class/drm/*HDMI*/modes; do
  [ -e "$f" ] || continue
  echo "--- $f ---" >> "$REPORT"
  cat "$f" >> "$REPORT" 2>/dev/null
  first_mode="$(head -n1 "$f" 2>/dev/null)"
  HDMI_MODES_SUMMARY="${HDMI_MODES_SUMMARY}${f}=${first_mode}; "
done
[ -n "$HDMI_STATUS_SUMMARY" ] || HDMI_STATUS_SUMMARY="no-hdmi-status-found"
[ -n "$HDMI_MODES_SUMMARY" ] || HDMI_MODES_SUMMARY="no-hdmi-modes-found"
append_json_pair "hdmi_status" "$HDMI_STATUS_SUMMARY"
append_json_pair "hdmi_modes_first" "$HDMI_MODES_SUMMARY"

# xrandr / xset
section "X DISPLAY" >> "$REPORT"
XRANDR_OUT="$(DISPLAY=:0 xrandr --query 2>&1)"
XSET_OUT="$(DISPLAY=:0 xset -q 2>&1)"
echo "--- xrandr ---" >> "$REPORT"
echo "$XRANDR_OUT" >> "$REPORT"
echo "--- xset -q ---" >> "$REPORT"
echo "$XSET_OUT" >> "$REPORT"
append_json_pair "xrandr" "$XRANDR_OUT"
append_json_pair "xset" "$XSET_OUT"

# Processes
section "GUI PROCESSES" >> "$REPORT"
PROC_OUT="$(ps -ef | grep -E 'Xorg|openbox|chromium|chrome' | grep -v grep 2>/dev/null)"
echo "$PROC_OUT" >> "$REPORT"
append_json_pair "gui_processes" "$PROC_OUT"

# systemd services
section "SYSTEMD SERVICES" >> "$REPORT"
SERVICES=(evck-gui evck-kiosk evck-monitor evck-orchestrator evck-ocpp evck-lpc evck-gpio)
SERVICE_SUMMARY=""
for s in "${SERVICES[@]}"; do
  state="$(systemctl is-active "$s" 2>/dev/null || true)"
  enabled="$(systemctl is-enabled "$s" 2>/dev/null || true)"
  echo "$s: active=$state enabled=$enabled" >> "$REPORT"
  SERVICE_SUMMARY="${SERVICE_SUMMARY}${s}[active=${state},enabled=${enabled}]; "
done
append_json_pair "services" "$SERVICE_SUMMARY"

# Temps
section "THERMALS" >> "$REPORT"
THERM_SUMMARY=""
for z in /sys/class/thermal/thermal_zone*; do
  [ -e "$z" ] || continue
  type="$(cat "$z/type" 2>/dev/null)"
  temp_raw="$(cat "$z/temp" 2>/dev/null || echo 0)"
  temp_c="$(awk "BEGIN {printf \"%.1f\", $temp_raw/1000}")"
  echo "$z type=$type temp_raw=$temp_raw temp_c=${temp_c}C" >> "$REPORT"
  THERM_SUMMARY="${THERM_SUMMARY}${type}=${temp_c}C; "
done
append_json_pair "thermals" "$THERM_SUMMARY"

# CPU freq
section "CPU FREQUENCY" >> "$REPORT"
CPUFREQ_SUMMARY=""
for cpu in /sys/devices/system/cpu/cpu[0-9]*; do
  [ -d "$cpu" ] || continue
  c="$(basename "$cpu")"
  cur="$(cat "$cpu/cpufreq/scaling_cur_freq" 2>/dev/null || echo n/a)"
  gov="$(cat "$cpu/cpufreq/scaling_governor" 2>/dev/null || echo n/a)"
  echo "$c cur_freq=$cur governor=$gov" >> "$REPORT"
  CPUFREQ_SUMMARY="${CPUFREQ_SUMMARY}${c}=${cur}/${gov}; "
done
append_json_pair "cpu_freq" "$CPUFREQ_SUMMARY"

# Memory / disk
section "MEMORY / DISK" >> "$REPORT"
FREE_OUT="$(free -h 2>/dev/null)"
DF_OUT="$(df -h / /opt /tmp 2>/dev/null)"
echo "--- free -h ---" >> "$REPORT"
echo "$FREE_OUT" >> "$REPORT"
echo "--- df -h ---" >> "$REPORT"
echo "$DF_OUT" >> "$REPORT"
append_json_pair "memory" "$FREE_OUT"
append_json_pair "disk" "$DF_OUT"

# Network
section "NETWORK" >> "$REPORT"
IP_OUT="$(ip -br a 2>/dev/null)"
ROUTE_OUT="$(ip route 2>/dev/null)"
PING1_OUT="$(ping -c 2 -W 2 1.1.1.1 2>&1 || true)"
PING8_OUT="$(ping -c 2 -W 2 8.8.8.8 2>&1 || true)"
DNS_OUT="$(getent hosts google.com 2>&1 || true)"

echo "--- ip -br a ---" >> "$REPORT"
echo "$IP_OUT" >> "$REPORT"
echo "--- ip route ---" >> "$REPORT"
echo "$ROUTE_OUT" >> "$REPORT"
echo "--- ping 1.1.1.1 ---" >> "$REPORT"
echo "$PING1_OUT" >> "$REPORT"
echo "--- ping 8.8.8.8 ---" >> "$REPORT"
echo "$PING8_OUT" >> "$REPORT"
echo "--- DNS lookup ---" >> "$REPORT"
echo "$DNS_OUT" >> "$REPORT"

append_json_pair "ip" "$IP_OUT"
append_json_pair "route" "$ROUTE_OUT"
append_json_pair "ping_1_1_1_1" "$PING1_OUT"
append_json_pair "ping_8_8_8_8" "$PING8_OUT"
append_json_pair "dns_lookup" "$DNS_OUT"

# GPU / GL
section "OPENGL / GPU" >> "$REPORT"
if have_cmd glxinfo; then
  GLX_OUT="$(DISPLAY=:0 glxinfo 2>/dev/null | grep -E 'OpenGL vendor|OpenGL renderer|OpenGL version' || true)"
else
  GLX_OUT="glxinfo not found"
fi
echo "$GLX_OUT" >> "$REPORT"
append_json_pair "glxinfo" "$GLX_OUT"

# Journal snippets
section "KERNEL / X / HDMI / THERMAL LOG TAIL" >> "$REPORT"
DMESG_OUT="$(dmesg -T 2>/dev/null | grep -Ei 'hdmi|drm|gpu|display|edid|thermal|thrott|overheat' | tail -n 100 || true)"
JOURNAL_GUI_OUT="$(journalctl -u evck-kiosk -u evck-gui -u evck-monitor -n 80 --no-pager 2>/dev/null || true)"
echo "--- dmesg filtered ---" >> "$REPORT"
echo "$DMESG_OUT" >> "$REPORT"
echo "--- service journal ---" >> "$REPORT"
echo "$JOURNAL_GUI_OUT" >> "$REPORT"
append_json_pair "dmesg_filtered" "$DMESG_OUT"
append_json_pair "service_journal" "$JOURNAL_GUI_OUT"

# Screenshot if possible
section "SCREENSHOT" >> "$REPORT"
SHOT_STATUS="not-attempted"
if have_cmd scrot; then
  if DISPLAY=:0 scrot "$SCREENSHOT" >/dev/null 2>&1; then
    SHOT_STATUS="ok:$SCREENSHOT"
  else
    SHOT_STATUS="failed-scrot"
  fi
elif have_cmd import; then
  if DISPLAY=:0 import -window root "$SCREENSHOT" >/dev/null 2>&1; then
    SHOT_STATUS="ok:$SCREENSHOT"
  else
    SHOT_STATUS="failed-import"
  fi
else
  SHOT_STATUS="no-screenshot-tool"
fi
echo "$SHOT_STATUS" >> "$REPORT"
append_json_pair "screenshot" "$SHOT_STATUS"

# Simple diagnosis
section "AUTO DIAGNOSIS" >> "$REPORT"
DIAGNOSIS=()

if echo "$HDMI_STATUS_SUMMARY" | grep -q "connected"; then
  DIAGNOSIS+=("HDMI link detected")
else
  DIAGNOSIS+=("HDMI link NOT detected")
fi

if echo "$XRANDR_OUT" | grep -qi " connected"; then
  DIAGNOSIS+=("X sees connected display")
else
  DIAGNOSIS+=("X does not clearly report connected display")
fi

if pgrep -x Xorg >/dev/null 2>&1; then
  DIAGNOSIS+=("Xorg running")
else
  DIAGNOSIS+=("Xorg not running")
fi

if pgrep -f chromium >/dev/null 2>&1 || pgrep -f chrome >/dev/null 2>&1; then
  DIAGNOSIS+=("Chromium running")
else
  DIAGNOSIS+=("Chromium not running")
fi

CPU_TEMP_RAW="$(cat /sys/class/thermal/thermal_zone2/temp 2>/dev/null || echo 0)"
CPU_TEMP_C="$(awk "BEGIN {printf \"%.1f\", $CPU_TEMP_RAW/1000}")"
if awk "BEGIN {exit !($CPU_TEMP_C >= 85.0)}"; then
  DIAGNOSIS+=("CPU temperature is VERY HIGH (${CPU_TEMP_C}C)")
elif awk "BEGIN {exit !($CPU_TEMP_C >= 75.0)}"; then
  DIAGNOSIS+=("CPU temperature is high (${CPU_TEMP_C}C)")
else
  DIAGNOSIS+=("CPU temperature is acceptable (${CPU_TEMP_C}C)")
fi

if echo "$PING1_OUT" | grep -q "0 received"; then
  DIAGNOSIS+=("No public internet reachability")
else
  DIAGNOSIS+=("Public internet reachable")
fi

for d in "${DIAGNOSIS[@]}"; do
  echo "- $d" >> "$REPORT"
done
append_json_pair "diagnosis" "$(printf '%s | ' "${DIAGNOSIS[@]}")"

echo "" >> "$REPORT"
echo "Saved report: $REPORT"
echo "Saved json:   $JSON"

printf '\n}\n' >> "$JSON"

echo "Healthcheck complete."
echo "Report: $REPORT"
echo "JSON:   $JSON"
[ -f "$SCREENSHOT" ] && echo "Shot:   $SCREENSHOT"
نحوه نصب
bash
sudo mkdir -p /opt/evck/tools
sudo nano /opt/evck/tools/evck-healthcheck.sh
محتوا را paste کن، بعد:

bash
sudo chmod +x /opt/evck/tools/evck-healthcheck.sh
اجرا:

bash
/opt/evck/tools/evck-healthcheck.sh
یا با مسیر خروجی دلخواه:

bash
/opt/evck/tools/evck-healthcheck.sh /opt/evck/logs/healthchecks
اگر خواستی screenshot هم بگیرد
اگر scrot نصب نیست:

bash
sudo apt-get update
sudo apt-get install -y scrot
بعد اسکریپت می‌تواند از صفحه فعلی خروجی PNG بگیرد.

این خیلی کمک می‌کند بفهمی “مانیتور خاموش است” یا “UI سیاه رندر شده”.

پیشنهاد ارتقای بعدی
من پیشنهاد می‌کنم نسخه دوم این اسکریپت را به شکل daemon/timer هم داشته باشی که هر 1 دقیقه:

دما
HDMI
X
Chromium
screenshot
آخرین journal
را ذخیره کند، و اگر:

HDMI از connected به disconnected رفت
X مرد
Chromium مرد
دما از 85 بالاتر رفت
فوراً یک snapshot ویژه بگیرد.

اگر خواستی، در پیام بعدی من همین الآن برات نسخه production-ready با systemd service + timer + log rotation + exit code استاندارد هم می‌نویسم تا مستقیم روی همه دستگاه‌ها deploy کنی.

آیا اسلیپ نداره ؟‌ که مثلا باید موس رو تکون بدیم تا روشن بشه.؟؟
خیر، طبق تمام لاگ‌ها و تنظیماتی که روی دستگاهت وجود دارد، Xorg و سیستم شما Sleep / DPMS / Screen Saver ندارند و هیچ‌چیزی به‌صورت نرم‌افزاری صفحه را خاموش نمی‌کند.

این را دقیقاً از خروجی xset -q می‌شود فهمید:

DPMS (Energy Star):

Standby: 600 Suspend: 600 Off: 600

DPMS is Disabled

توضیح مهم:

اینکه Standby/Suspend/Off عدد دارند مهم نیست
وقتی نوشته: DPMS is Disabled → کل قابلیت خاموش کردن صفحه غیرفعال است
یعنی Xorg اجازه ندارد مانیتور را standby / sleep / power-off کند
همچنین timeout: 0 → screen saver هم عملاً غیرفعال است
بنابراین:

سیستم نمی‌تواند به‌صورت خودکار صفحه را خاموش کند
نیازی نیست موس تکان داده شود
نیازی نیست صفحه را با ورودی بیدار کنید
چون اصلاً هیچ sleep فعال نیست.

نتیجه نهایی
به‌هیچ‌وجه Sleep/Standby/ScreenBlank خودکار فعال نیست.

بنابراین اگر صفحه سیاه می‌شود یا “خاموش به نظر می‌رسد”، علت نرم‌افزاری نیست و فقط یکی از این‌ها می‌تواند باشد:

1) ناپایداری GPU به خاطر دمای بالا (محتمل)
(85°C برای Orange Pi Zero 2W خیلی بالاست)

2) مانیتور خودش sleep یا protective mode می‌رود
(وقتی چند لحظه سیگنال ناپایدار ببیند)

3) خطای لحظه‌ای در GPU Process کرومیوم
(وقتی GPU تحت فشار حرارتی یا منابع باشد)

4) افت ولتاژ پاور برد
(یکی از شایع‌ترین دلایل black flicker در SBCها)

5) مشکل کیفیت کابل HDMI یا EMI
(در دما یا فشار تغییر می‌کند)

اگر خواستی، می‌توانم همین الان یک تست خیلی ساده بدهم که متوجه بشوی آیا مانیتور خودش می‌رود sleep، یا GPU تصویر را سیاه می‌کند.

یا اگر خواستی، یک اسکریپت بدهم که هر ثانیه HDMI status و GPU temp و GPU process را چک کند تا لحظه‌ی وقوع، دلیل کشف شود.

ارتقا بعدی رو مفصل توضیح بده
حتماً. منظور از «ارتقای بعدی» این بود که به‌جای یک healthcheck دستی، یک سیستم مانیتورینگ دائمی و خودکار بسازیم که هر وقت مشکل رخ داد، همان لحظه مدرک جمع کند.

هدف ارتقای بعدی
اگر صفحه سیاه شد یا GUI مشکل پیدا کرد، این سیستم باید:

خودش هر 1 دقیقه یا هر چند ثانیه چک کند
وضعیت HDMI / Xorg / Chromium / دما / فرکانس / شبکه را ذخیره کند
اگر anomaly دید، فوری snapshot بگیرد
لاگ‌های قبل و بعد از حادثه را نگه دارد
بعداً بتوانی دقیق بفهمی:
مشکل از گرما بوده؟
از HDMI بوده؟
از Chromium بوده؟
از پاور بوده؟
از شبکه بوده؟
یا از مانیتور؟
معماری پیشنهادی
1) یک اسکریپت “collector”
این اسکریپت هر بار اجرا می‌شود و یک نمونه snapshot می‌سازد:

timestamp
HDMI status
xrandr
xset
processes
thermals
cpu freq
network
journal tail
screenshot
خروجی:

یک فایل txt
یک فایل json
در صورت امکان یک png
2) یک systemd timer
به‌جای اینکه دستی اجرا شود، systemd آن را مثلاً هر 60 ثانیه اجرا می‌کند.

مزیت:

ساده
reliable
لاگ‌دار
بعد از reboot هم خودکار می‌آید بالا
3) یک “watchdog logic”
اگر شرایط خطرناک شد، snapshot ویژه بگیرد.

مثلاً اگر:

HDMI از connected به disconnected رفت
Xorg قطع شد
Chromium dead شد
دما از 85 گذشت
network down شد
آن وقت:

snapshot فوری
screenshot فوری
journal بیشتر
maybe service restart notification
چرا این کار مهم است؟
چون مشکل تو intermittent است.

مشکل‌های intermittent معمولاً وقتی لاگ می‌گیری، دیگر دیده نمی‌شوند.

یعنی:

الان مانیتور سیاه شده
30 ثانیه بعد خودبه‌خود برگشته
وقتی تو دستور می‌زنی، همه‌چیز سالم است
پس باید قبل از رخداد data جمع شود، نه بعد از آن.

اجزای دقیق سیستم مانیتورینگ
A) snapshot periodic
مثلاً هر دقیقه:

/sys/class/drm/*HDMI*/status
xrandr --query
xset -q
ps -ef | grep chromium
thermal_zone*
scaling_cur_freq
ping 1.1.1.1
ping 8.8.8.8
journalctl -u evck-kiosk -n 30
dmesg | tail
این snapshot ها با timestamp ذخیره می‌شوند.

B) event-based snapshot
اگر یکی از شرط‌های زیر رخ داد:

CPU > 85C
HDMI disconnected
Xorg unavailable
Chromium process missing
ping fail
screenshot black
rendering issue
فوراً یک snapshot کامل‌تر بگیر.

C) retention / rotation
چون اگر هر دقیقه snapshot بگیری، فایل‌ها زیاد می‌شوند.

پس باید:

فقط 7 روز اخیر نگه داشته شود
یا حجم بیش از 500MB شد، پاکسازی شود
یا zip/compress شود
طراحی عملی پیشنهادی
روش ساده و خوب
سه فایل:

1) collector script
مثلاً:

/opt/evck/tools/evck-healthcheck.sh

2) systemd service
/etc/systemd/system/evck-healthcheck.service

3) systemd timer
/etc/systemd/system/evck-healthcheck.timer

نمونه behavior
هر دقیقه:

snapshot normal گرفته شود
اگر thermal بالا بود:

snapshot special with screenshot
journal tail بیشتر
maybe vcgencmd یا ابزار حرارتی خاص
اگر HDMI lost شد:

فوری report جداگانه با severity=critical
پیشنهاد ارتقای هوشمندتر
بهتر است اسکریپت فقط “گزارش” ندهد، بلکه تشخیص اولیه هم بکند.

مثلاً خروجی diagnosis:

OK: HDMI connected
WARN: CPU temp high
CRIT: Chromium missing
CRIT: GPU unstable suspected
WARN: no internet
یعنی در هر snapshot یک severity داشته باشیم.

ساختار فولدر پیشنهادی
bash
/opt/evck/logs/healthchecks/
  ├── 20260519-120001.txt
  ├── 20260519-120001.json
  ├── 20260519-120001.png
  ├── 20260519-120101.txt
  ├── 20260519-120101.json
  └── ...
و یک symlink هم داشته باشیم برای آخرین snapshot:

bash
/opt/evck/logs/healthchecks/latest.txt
/opt/evck/logs/healthchecks/latest.json
/opt/evck/logs/healthchecks/latest.png
اگر بخواهی حرفه‌ای‌تر شود
می‌شود این‌ها را هم اضافه کرد:

1) watchdog service
اگر اسکریپت دید UI مرده:

service restart
log it
snapshot before/after restart
2) temperature threshold alert
اگر دما بالای 80 رفت:

log warning
اگر 85 رسید:
critical snapshot
شاید alert به backend
3) screenshot analyzer
اگر screenshot تقریباً black شد:

ثبت به عنوان احتمال black screen
این خیلی مفید است برای تشخیص مشکل نمایش
4) remote upload
خروجی‌ها را به یک server مرکزی هم بفرستیم:

scp
rsync
HTTP POST
MQTT
پیشنهاد عملی من برای تو
برای همین پروژه، بهترین نسخه این است:

healthcheck script
systemd timer هر 60 ثانیه
snapshot هنگام critical condition
retention 7 روز
آخرین report + screenshot همیشه نگه داشته شود
نتیجه
این ارتقا باعث می‌شود دیگر مجبور نباشی لحظه‌ای که مشکل رخ می‌دهد دستی دنبال لاگ بگردی.

سیستم خودش مدرک جمع می‌کند.

اگر خواستی، در پیام بعدی من می‌توانم همین الان نسخه کامل production-ready را بدهم شامل:

evck-healthcheck.sh
evck-healthcheck.service
evck-healthcheck.timer
logrotate یا cleanup script
restart logic
severity levels
اگر بخواهی، من کل پکیج را یکجا می‌نویسم.




orangepi@orangepizero2w:~$ sudo nano /opt/evck/tools/evck-healthcheck.sh
orangepi@orangepizero2w:~$ sudo chmod +x /opt/evck/tools/evck-healthcheck.sh
orangepi@orangepizero2w:~$ /opt/evck/tools/evck-healthcheck.sh
Saved report: /opt/evck/logs/healthchecks/health-orangepizero2w-20260519-121858.txt
Saved json:   /opt/evck/logs/healthchecks/health-orangepizero2w-20260519-121858.json
Healthcheck complete.
Report: /opt/evck/logs/healthchecks/health-orangepizero2w-20260519-121858.txt
JSON:   /opt/evck/logs/healthchecks/health-orangepizero2w-20260519-121858.json
Shot:   /opt/evck/logs/healthchecks/screenshot-orangepizero2w-20260519-121858.png
orangepi@orangepizero2w:~$ sudo cat /opt/evck/logs/healthchecks/health-orangepizero2w-20260519-121858.json
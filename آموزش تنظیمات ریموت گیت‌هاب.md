این آموزش رو آپدیت کردم با موارد مربوط به cleanup که الان انجام دادی:

---

# آموزش کامل راه‌اندازی و تنظیم Git و SSH برای GitHub

این آموزش شما را مرحله به مرحله ازساخت کلید SSH گرفته تا تنظیم Git برای کار باریپازیتوری‌های GitHub راهنمایی می‌کند.

## بخش ۱: ایجاد و تنظیم کلید SSH

کلید SSH به شما اجازه می‌دهد بدون نیاز به وارد کردن نام کربری و رمز عبور، به صورت امن به GitHub متصل شوید.

### مرحله ۱.۱: بررسی وجود کلید SSH

اول ببینیم آیا از قبل کلید SSH دارید یا نه.

*   **دستور (در PowerShell یا Git Bash):**
    ```bash
    ls ~/.ssh
    ```
*   **خروجی مورد انتظار:**
    اگر کلید دارید، فایل‌هایی با نام‌های شبیه `id_rsa.pub` یا `id_ed25519.pub` (کلید عمومی) و `id_rsa` یا `id_ed25519` (کلید خصوصی) را خواهید دید. فایل `.pub` کلید عمومی شماست که باید در GitHub وارد شود.
*   **اگر کلید ندارید:** به مرحله ۱.۲ بروید. اگر دارید، می‌توانید به مرحله ۱.۳ بروید تا ببینید کدام کلید را می‌خواهید استفاده کنید.

### مرحله ۱.۲: ساخت کلید SSH جدید

اگر کلید SSH ندارید، یکی بسازید. ما از `ed25519` استفاده می‌کنیم که امن و جدید است.

*   **دستور:**
    ```bash
    ssh-keygen -t ed2519 -C "your_email@example.com"
    ```
    *   `"your_email@example.com"` را بایمیل GitHub خودتان جایگزین کنید.
*   **پیام‌ها:**
    *   `Enter file in which to save the key (...)`: اگر می‌خواهید نام پیش‌فرض (`id_ed25519`) را نگه دارید، فقط `Enter` را بزنید. اگر می‌خواهید نام خاصی مثل `id_ed25519_farhad` داشته باشد، نام دلخواه را وارد کنید. **مهم:** اگر نامی متفاوت از پیش‌فرض انتخاب می‌کنید، حتماً در مراحل بعدی در فایل `config` و دستورات Git از همین نام استفاده کنید.
    *   `Enter passphrase (empty for no passphrase):`: یک رمز عبور قوی وارد کنید (برای امنیت بشتر). این رمز عبور را هنگام استفاده از کلید از شما خواسته می‌شود. اگر خالی بگذارید، نیازی به وارد کردن رمز نخواهید داشت.
*   **نتیجه:** دو فایل در پوشه `~/.ssh` شما ایجاد می‌شود: `id_ed25519` (کلید خصوصی) و `id_ed25519.pub` (کلید عمومی).

### مرحله ۱.۳: اضافه کردن کلید SSH به SSH Agent

SSH Agent کلیدهای شما را در حافظه نگه می‌دارد تا مجبور نباشید هر بار رمز عبور را وارد کنید.

*   **دستور (برای شروع agent):**
    ```bash
    eval "$(ssh-agent -s)"
    ```
*   **دستور (برای اضافه کردن کلید):**
    ```bash
    ssh-add ~/.ssh/id_ed25519
    ```
    *   اگر نام کلید شما متفاوت است، نام صحیح را به جای `id_ed25519` وارد کنید (مثلاً `ssh-add ~/.ssh/id_ed25519_farhad`).
*   **پیام:** اگر هنگام ساخت کلید رمز عبور گذاشته باشید، اینجا از شما خواسته می‌شود آن را وارد کنید.

---

## بخش ۲: تنظیم فایل `config` مربوط به SSH

این فایل به SSH می‌گوید که برای هر هاست (مثل GitHub) از کدام کلید و با چه تنظیماتی استفاده کند.

### مرحله ۲.۱: ویرایش فایل `~/.ssh/config`

*   **دستور (برای مشاهده ویرایش):**
    ```bash
    cat ~/.ssh/config
    ```
    اگر فایل وجود ندارد، آن را با ویرایشگر متن بسازید (مثلاً `notepad ~/.ssh/config` در PowerShell).
*   **اضافه کردن تنظیمات برای GitHub:**
    بخش‌های زیر را به فایل `config` اضافه کنید. **حتماً** نام فایل کلید خصوصی (`IdentityFile`) را با نام کلیدی که انتخاب کردید (در مرحله ۱.۲) تطبیق دهید.

    ```shconfig
    # --- تنظیمات General ---
    Host *
        ServerAliveInterval 30
        ServerAliveCountMax 6
        TCPKeepAlive yes
        ConnectTimeout 10
        StrictHostKeyChecking accept-new # برای اولین اتصال‌ها راحت‌تر است، در محیط‌های امن‌تر می‌توانید این را تغییر دهید

    # --- تنظیمات برای GitHub با کلید شخصی Farhad ---
    Host github-farhad
      HostName github.com
      User git
      IdentityFile C:\Users\Danesh\.ssh\id_ed25519 # <-- نام کلید خصوصی خود را اینجا وارد کنید

    # --- تنظیمات برای GitHub با کلید Sara ---
    Host github-sara
      HostName github.com
      User git
      IdentityFile C:\Users\Danesh\.ssh\id_ed25519_sara # <-- نام کلید خصوصی Sara را اینجا وارد کنید
    ```
    *   **توضیح:**
        *   `Host`: نام مستعاری است که شما در دستورات Git استفاده خواهید کرد (مثلاً `github-farhad`).
        *   `HostName`: آدرس واقعی سرور GitHub است.
        *   `User`: نام کاربری برای اتصال SSH به GitHub همیشه `git` است.
        *   `IdentityFile`: مسیر دقیق فایل کلید خصوصی شما.

### مرحله ۲.۲: تست اتصال SSH با Alias

حالا باید بتوانید با استفاده از alias ها به GitHub وصل شوید.

*   **دستور (برای alias `github-farhad`):**
    ```bash
    ssh -T git@github-farhad
    ```
*   **دستور (برای alias `github-sara`):**
    ```bash
    ssh -T git@github-sara
    ```
*   **خروجی موفق:**
    شما باید پیام شبیه این ببینید:
    `Hi farhad3113teslapower! You've successfully authenticated, but GitHub does not provide shell access.`
    (یا نام کاربری مربوط به `github-sara`).
*   **اگر خطا گرفتید:** دوباره مراحل ۱ و ۲ را چک کنید، مخصوصاً نام کلید در `IdentityFile` و اینکه کلید در SSH Agent اضافه شده باشد.

---

## بخش ۳: تنظیم Git Remotes

حالا که SSH آماده است، باید Git را تنظیم کنیم تا از اینتصالات استفاده کند.

### مرحله ۳.۱: حذف Remotes نامعتبر

در پروژه `evck160-decentralized`، ما یک remote به نام `github` داریم که به `github-work` اشاره می‌کند (که در `config` تعریف نشده) و یک remote به نام `origin` که به یک مسیر محلی نامعتبر اشاره دارد.

*   **دستور (برای حذف `github`):**
    ```bash
    git remote remove github
    ```
*   **دستور (برای حذف `origin`):**
    ```bash
    git remote remove origin
    ```

### مرحله ۳.۲: اضافه کردن Remotes صحیح

حالا باید remotes را با URL های صحیح و alias های درست SSH اضافه کنیم.

*   **دستور (برای اضافه کردن remote به نام `github` با استفاده از `github-farhad`):**
    ```bash
    git remote add github git@github-farhad:farhad3113teslapower/evck160c.git
    ```
    *   این دستور یک remote به نام `github` می‌سازد که به ریپازیتوری شما در GitHub با استفاده از alias `github-farhad` وصل می‌شود.
*   **دستور (برای اضافه کردن remote به نام `origin` با استفاده از `github-farhad`):**
    (معولاً `origin` را به عنوان remote اصلی استفاده می‌کنیم)
    ```bash
    git remote add origin git@github-farhad:farhad3113teslapower/evck160c.git
    ```
    *   اگر می‌خواهید `origin` به یک ریپازیتوری دیگر اشاره کند، URL آن را درستور بالا جایگزین کنید.

### مرحله ۳.۳: مشاهده و تاید Remotes

*   **دستور:**
    ```bash
    git remote -v
    ```
*   **خروجی مورد انتظار:**
    ```
    github  git@github-farhad:farhad3113teslapower/evck160c.git (fetch)
    github  git@github-farhad:farhad3113teslapower/evck160c.git (push)
    origin  git@github-farhad:farhad3113teslapower/evck160c.git (fetch)
    origin  git@github-farhad:farhad3113teslapower/evck160c.git (push)
    ```
    *   الان هر دو remote ( `github` و `origin`) به درستی از alias `github-farhad` استفاده می‌کند. remote به نام `github-farhad` که از HTTPS استفاده می‌کرد، همچنان باقی مانده و می‌توانید آن را هم نگه دارید یا حذف کنید (`git remote remove github-farhad`).

### مرحله ۳.۴: Fetch کردن از Remotes

حالا باید بتوانید اطلاعات را از GitHub دانلود کنید.

*   **دستور:**
    ```bash
    git fetch --all
    ```
*   **خروجی موفق:**
    دیگر نباید خطای `Could not resolve hostname github-work` را ببینید. به جای آن، لیستی از شاخه‌ها (branches) که از `github` و `origin` دانلود شده‌اند را خواهید دید.

---

## بخش ۴: تنظیم Upstream Branchها

این بخش اطمینان حاصل می‌کند که شاخه‌های محلی شما (local branches) می‌داند به کدام شاخه در remote (مثلاً `origin/main`) متصل هستند.

### مرحله ۴.۱: مشاهده Upstream ها

*   **دستور:**
    ```bash
    git branch -v
    ```
*   **خروجی:**
    شاخه‌های محلی شما را به همراه شاخه‌های remote که به آن‌ها متصل هستند نشان می‌دهد. برای مثال:
    ```
      bug/lpc_interface 1a2b3c4 [origin/bug/lpc_interface] Fix for LPC interface issue
    * main              d5e6f7g [origin/main] Update documentation
    ```
    علامت `*` نشان‌دهنده شاخه فعلی شماست. `[origin/main]` یعنی شاخه `main` محلی شما به شاخه `main` در remote به نام `origin` متصل است.

### مرحله ۴.۲: تنظیم Upstream (در صورت نیاز)

اگر خروجی `git branch -vv` نشان داد که شاخه‌ها به درستی متصل نیستند (مثلاً شاخه‌ای به `github/main` متصل استولی شما می‌خواهید به `origin/main` متصل باشد)، می‌توانید آن را تنظیم کنید.

*   **دستور (برای تنظیم upstream شاخه فعلی):**
    فرض کنید الان روی شاخه `main` هستید و می‌خواهید به `origin/main` متصل شود:
    ```bash
    git branch --set-upstream-to=origin/main
    ```
    *   اگر شاخه دیگری را می‌خواهید تنظیم کنید، ابتدا به آن شاخه بروید (`git checkout<branch-name>`) و سپس دستور بالا را اجرا کنید.

---

## بخش ۵: نظافت و مرتب‌سازی Repository (Cleanup)

با گذشت زمان، branch های قدیمی، worktree های رها شده، و remote های اضافه جمع می‌شوند. این بخش نشان می‌دهد چطور repo را تمیز نگه دارید.

### مرحله ۵.۱: مشاهده همه Branch ها (محلی و Remote)

*   **دستور:**
    ```cmd
    git branch -a
    ```
*   **خروجی:** لیست همه branch های local و remote را نشان می‌دهد.
    ```
    * main
      remotes/origin/HEAD -> origin/main
      remotes/origin/main
    ```

### مرحله ۵.۲: حذف Branch های محلی منسوخ

وقتی یک branch دیگر نیاز ندارید (مثلاً merge شده یا رها شده)، آن را حذف کنید.

*   **دستور (حذف ایمن — فقط اگر merge شده باشد):**
    ```cmd
    git branch -d branch-name
    ```
*   **دستور (حذف اجباری — حتی اگر merge نشده باشد):**
    ```cmd
    git branch -D branch-name
    ```
    > **هشدار:** `-D` بدون تأیید حذف می‌کند. مطمئن شوید کار آن branch تمام شده.

*   **حذف چند branch با هم (CMD):**
    ```cmd
    git branch -D branch1 branch2 branch3
    ```

### مرحله ۵.۳: هماهنگ‌سازی Remote های حذف‌شده از GitHub

اگر یک branch روی GitHub حذف شده ولی هنوز در لیست remote های محلی دیده می‌شود، با این دستور sync کنید:

*   **دستور (برای همه remote ها):**
    ```cmd
    git fetch --prune
    ```
*   **دستور (برای یک remote خاص):**
    ```cmd
    git fetch --prune origin
    git fetch --prune github-farhad
    ```
*   **خروجی نمونه (وقتی چیزی پاک می‌شود):**
    ```
    From github.com:farhad3113teslapower/evck160c-decentralized
     - [deleted]         (none)     -> origin/adaptingsettings
     - [deleted]         (none)     -> origin/bug/lpc_interface
    ```

### مرحله ۵.۴: بررسی و پاکسازی Worktree ها

Worktree یک کپی کاری موازی از repo است. اگر رها شده باشد، باید پاک شود.

*   **دستور (مشاهده worktree ها):**
    ```cmd
    git worktree list
    ```
*   **خروجی نمونه (worktree معتبر):**
    ```
    C:/path/to/repo  abc1234 [main]
    ```
*   **خروجی نمونه (worktree خراب — مسیر وجود ندارد):**
    ```
    C:/path/to/old-worktree  abc1234 [worktree-2026-01-26T08-13-05]  prunable
    ```
*   **دستور (پاک کردن worktree های خراب):**
    ```cmd
    git worktree prune
    ```این دستور فقط worktree هایی را پاک می‌کند که مسیرشان دیگر وجود ندارد (prunable). ایمن است.

### مرحله ۵.۵: حذف Remote های تکراری

اگر چند remote به یک URL یکسان اشاره می‌کنند، فقط `origin` را نگه دارید:

*   **دستور (بررسی URL های remote):**
    ```cmd
    git remote -v
    ```
*   **اگر دو remote URL یکسانی داشتند:**
    ```cmd
    git remote remove github
    git remote remove github-farhad
    ```
    فقط `origin` را نگه دارید که استاندارد Git است.

### مرحله ۵.۶: تأیید نهایی وضعیت Repository

بعد از هر cleanup، این سه دستور را اجرا کنید:

*   **دستور:**
    ```cmd
    git status
    git branch -a
    git remote -v
    ```
*   **خروجی ایده‌آل:**
    ```
    On branch main
    Your branch is up to date with 'origin/main'.
    nothing to commit, working tree clean
    ```
    ```
    * main
      remotes/origin/HEAD -> origin/main
      remotes/origin/main
    ```
    ```
    origin  git@github-farhad:farhad3113teslapower/evck160c-decentralized.git (fetch)
    origin  git@github-farhad:farhad3113teslapower/evck160c-decentralized.git (push)
    ```

---

## بخش ۶: تنظیم Upstream Branch ها

این بخش اطمینان حاصل می‌کند که شاخه‌های محلی شما (local branches) می‌دانند به کدام شاخه در remote (مثلاً `origin/main`) متصل هستند.

### مرحله ۶.۱: مشاهده Upstream ها

*   **دستور:**
    ```bash
    git branch -v
    ```
*   **خروجی:**
    شاخه‌های محلی شما را به همراه شاخه‌های remote که به آن‌ها متصل هستند نشان می‌دهد. برای مثال:
    ```
      bug/lpc_interface 1a2b3c4 [origin/bug/lpc_interface] Fix for LPC interface issue
    * main              d5e6f7g [origin/main] Update documentation
    ```
    علامت `*` نشان‌دهنده شاخه فعلی شماست. `[origin/main]` یعنی شاخه `main` محلی شما به شاخه `main` در remote به نام `origin` متصل است.

### مرحله ۶.۲: تنظیم Upstream (در صورت نیاز)

اگر خروجی `git branch -v` نشان داد که شاخه‌ها به درستی متصل نیستند (مثلاً شاخه‌ای به `github/main` متصل است ولی شما می‌خواهید به `origin/main` متصل باشد)، می‌توانید آن را تنظیم کنید.

*   **دستور (برای تنظیم upstream شاخه فعلی):**
    فرض کنید الان روی شاخه `main` هستید و می‌خواهید به `origin/main` متصل شود:
    ```bash
    git branch --set-upstream-to=origin/main
    ```
    *   اگر شاخه دیگری را می‌خواهید تنظیم کنید، ابتدا به آن شاخه بروید (`git checkout <branch-name>`) و سپس دستور بالا را اجرا کنید.

---

## خلاصه دستورات کلیدی

**راه‌اندازی SSH:**
1.  `ssh-keygen -t ed25519 -C "your_email@example.com"` — ساخت کلید SSH
2.  `eval "$(ssh-agent -s)"` — راه‌اندازی SSH Agent
3.  `ssh-add ~/.ssh/id_ed2519` — اضافه کردن کلید به Agent
4.  `cat ~/.ssh/config` — مشاهده و ویرایش فایل config
5.  `ssh -T git@github-farhad` — تست اتصال SSH با alias

**مدیریت Remote:**
6.  `git remote -v` — نمایش همه remotes
7.  `git remote add <name> <url>` — اضافه کردن remote جدید
8.  `git remote remove <name>` — حذف یک remote
9.  `git remote set-url <name> <url>` — تغییر URL یک remote موجود

**Cleanup و نظافت:**
10. `git branch -a` — نمایش همه branch ها (local و remote)
11. `git branch -d <name>` — حذف ایمن branch (فقط اگر merge شده)
12. `git branch -D <name>` — حذف اجباری branch
13. `git fetch --prune` — sync کردن و پاک کردن remote های حذف‌شده
14. `git fetch --prune <remote>` — همان کار برای یک remote خاص
15. `git worktree list` — نمایش worktree ها
16. `git worktree prune` — پاک کردن worktree های خراب

**بررسی وضعیت:**
17. `git status` — وضعیت فعلی repo
18. `git branch -v` — نمایش branch ها و upstream آن‌ها
19. `git fetch --all` — دانلود اطلاعات از همه remotes
20. `git branch --set-upstream-to=<remote>/<branch>` — تنظیم upstream برای branch فعلی

---

با انجام این مراحل، باید بتوانید به صورت کاملاً امن و صحیح با ریپازیتوری‌های GitHub خود کار کنید. اگر در هر مرحله‌ای با خطا مواجه شدید یا نیاز به راهنمایی بیشتری داشتید، بپرسید!